<resources>
    <style name="Theme.Estudario" parent="android:style/Theme.Material.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowActionModeOverlay">true</item>
    </style>

    <style name="Theme.Estudario.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <!-- O livro folheia uma página ao abrir (Android 12+); antes disso aparece parado. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_animated</item>
        <item name="windowSplashScreenAnimationDuration">880</item>
        <item name="postSplashScreenTheme">@style/Theme.Estudario</item>
    </style>
</resources>
